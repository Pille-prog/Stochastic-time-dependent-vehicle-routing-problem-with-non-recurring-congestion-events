# Instance data (see data/README.md for how to obtain it)
data/*
!data/README.md

# Local secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# Experiment outputs
Training_Graph_Folder/
runs/
Results_*.txt
Results_*.png
# Crash-recovery checkpoints written per episode by the long capture drivers
# (scripts/capture_*_w_trajectory.py). The finished run beside them is the
# artifact; these are a duplicate prefix of it.
*.partial.json
